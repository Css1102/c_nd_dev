services:
  - type: web
    name: fastapi-service
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host=0.0.0.0 --port=10000
    workingDir: fastapi-service
    envVars:
      - key: ENV
        value: production